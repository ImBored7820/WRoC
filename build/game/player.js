export class Player {
    //x:  number;
    //y: number;
    //speed: number ;
    // TODO Player should move
    draw(ctx) {
        ctx.fillStyle = "red";
        ctx.beginPath();
        //ctx.arc(this.x, this.y, 10, 0, 360);
        ctx.fill();
    }
}
